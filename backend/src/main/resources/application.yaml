spring:
  datasource:
    url: jdbc:mysql://localhost:3306/vendor_db
    username: root
    password: root   # change if needed

  jpa:
    hibernate:
      ddl-auto: update

  flyway:
    enabled: false